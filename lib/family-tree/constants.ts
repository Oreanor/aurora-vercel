/** Tree view: main node and sibling bead row width/height (px). */
export const TREE_NODE_WIDTH = 200;
export const TREE_NODE_HEIGHT = 180;
export const TREE_BEAD_WIDTH = 72;

export const BRANCH_LEVEL_SPACING = 250;
export const BRANCH_MIN_SPOUSE_DISTANCE = 200;
/** Width of one marriage block in sibling branch (spouse + rings), for multiple marriages. */
export const BRANCH_MARRIAGE_BLOCK_WIDTH = BRANCH_MIN_SPOUSE_DISTANCE + TREE_NODE_WIDTH;
export const BRANCH_ANCHOR_OFFSET_X = 280;
